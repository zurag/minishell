#include "../includes/minishell.h"

int	main()
{
	// char *line = readline(NULL);
	// while (line)
	// {
	// 	printf("%s\n", line);
	// 	free (line);
	// 	line = readline(NULL);
	// }
	// free (line);
	// printf("%s\n", getenv("PATH"));
	



	return (0);
}
